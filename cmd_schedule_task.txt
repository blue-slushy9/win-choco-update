# This command allows you to run a .bat script on a schedule, in this case
# every Saturday at midnight

# ISSUES: there is a section in the batch script where it opens up a separate
# cmd window as a user instead of as admin, the script prompts for the user's
# password at that point. I'm not sure what will happen during that step if
# the script is run as a scheduled task.

schtasks /create /tn "win_choco_update.bat" /tr "C:\Users\Me\documents\GitHub\PowerShell\win-choco-update\win_choco_update.bat" /sc weekly /d SAT /st 00:00 /RL HIGHEST